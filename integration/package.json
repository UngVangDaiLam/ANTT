{
  "name": "@secure-notes/integration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Test tích hợp: client-sdk thật (mật mã thật) nói chuyện với server thật qua HTTP. Không chứa code chạy thật.",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@secure-notes/client-sdk": "workspace:*",
    "@secure-notes/server": "workspace:*",
    "@secure-notes/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^5.0.1"
  }
}
